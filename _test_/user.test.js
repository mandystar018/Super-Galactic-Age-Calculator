import User from '../src/js/user.js';

  describe('TODO', () => {
  test('should correctly create a triangle object with three lengths', () => {
    const TODO = new TODO();
    expect(TODO).toEqual();
    expect(TODO).toEqual();
    expect(TODO).toEqual();
  });
});